{
  "scripts": {
    "client": "cd Client-Complexity-Analyzer && npm install && npm run start",
    "server": "cd Server-Complexity-Analyzer && npm install && npm run start:watch",
    "dev": "npm install && concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}